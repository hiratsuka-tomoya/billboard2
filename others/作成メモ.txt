役職は部署に含めて部署テーブルに
1総務
2情報セキュ
3支店長
4社員
で登録していいってさ

インデクッスはホーム画面

loginServletで(User)loginUserをセッションに登録し、以後それを参照してログイン状態を判断（Nullなら非ログイン）

多重ログイン

					<fmt:formatDate value="${message.insertDate}"
						pattern="yyyy/MM/dd HH:mm:ss" />
						
						エンティティに対する操作を格納するクラスをサービス
					
セッションオブジェクトを破棄しているタイミングを把握できていない

session.setAttribute
と
request.set---の使い分け


セッションオブジェクト破棄しないと複数続けて登録したときまずくなりそう。要確認。

get と　post の違いがまだいまいち・・サーバーやクライアントのどこでどうデータをやり取りや保持しているのかっていう概念がつかめていない


getParameterで取得する値は
name="****"だよね？でもnameってjsp内で重複できるよね。どれを取得するの？

SQLの SELECT WHERE について、カラム名、条件、 比較演算内容 の3要素を持つビーンを作成し
そのリストを渡して、要素数回ループして指定するゲットメソッドを用意すれば
めっちゃ簡略化できるっしょ

SQLをアップデートしたらビーンもアップデートしなきゃいけないことがあるはず
（ログインしているユーザーの情報を変更したときとか）

MVCモデル復習する

isStoppedはじく